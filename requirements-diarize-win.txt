# Windows diarization test environment
# Use in a separate virtual environment. Do not install into the main app environment.
# Python 3.10 recommended.

# Core app dependencies
Flask
Flask-Cors
psutil
numpy<2
torch==2.1.2
# torchvision and torchaudio should match the selected torch build
# For CUDA 12.1:
# torchvision==0.16.2+cu121
# torchaudio==2.1.2+cu121
# For CPU-only testing:
# torchvision==0.16.2
# torchaudio==2.1.2
openai-whisper
easyocr
transformers
accelerate
ollama
Pillow
langchain
langgraph
langchain-ollama
openpyxl

# Diarization stack
pyannote.audio==3.1.1
git+https://github.com/m-bain/whisperx.git

# Install k2 from the official matching wheel for your Python/torch build.
# See: https://k2-fsa.github.io/k2/cpu.html
# Example for Windows CPU + Python 3.10 / torch 2.1.2:
# pip install "k2==1.24.3.dev20230726+cpu.torch2.1.2" -f https://k2-fsa.github.io/k2/cpu.html
# Exact wheel may differ by torch version. Match the official k2 build to your environment.
