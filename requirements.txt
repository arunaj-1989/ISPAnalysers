--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

# Build dependency
wheel # Ensures pip can build wheels from source

# OCR dependency
easyocr

# Core dependencies for streaming Tamil-to-English translation with Whisper on GPU.
# Use stable versions of torch compiled for CUDA 12.1
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1
openai-whisper
sounddevice
numpy
scipy
numba
llvmlite

# Streamlit chat UI and browser microphone streaming.
streamlit>=1.33.0
streamlit-webrtc>=0.47.0
streamlit-autorefresh==1.0.1
indic-transliteration>=2.3.0
ollama==0.2.1
